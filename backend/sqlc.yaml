version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/platform/db/query.sql"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "internal/platform/db"
        sql_package: "pgx/v5"
        emit_interface: true
