version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # GetSession intentionally returns Session rather than GetSessionResponse.
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
