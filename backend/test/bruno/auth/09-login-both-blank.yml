info:
  name: 09 Login Both Fields Blank
  type: http
  seq: 9

http:
  method: POST
  url: "{{host}}/auth.v1.AuthService/Login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": ""
      }

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  assertions:
    - expression: res.body.code
      operator: eq
      value: "invalid_argument"

docs: |
  Login table case: both fields blank -> invalid_argument.
  Replaces: login_cases.json row "both fields blank" in run_auth_flow.sh.
