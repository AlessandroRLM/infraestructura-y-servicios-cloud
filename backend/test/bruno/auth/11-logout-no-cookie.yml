info:
  name: 11 Logout Without Cookie
  type: http
  seq: 11

http:
  method: POST
  url: "{{host}}/auth.v1.AuthService/Logout"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {}

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  assertions:
    - expression: res.body.code
      operator: eq
      value: "unauthenticated"

docs: |
  Session lifecycle: logout without cookie -> unauthenticated.
  Replaces: call Logout '{}' (no cookie) in run_auth_flow.sh.
  Bash check: "logout without cookie rejected"
