info:
  name: 44 Outcome SE-A Still Passed After Boundary Correction
  type: http
  seq: 44

http:
  method: POST
  url: "{{host}}/section_enrollment.v1.SectionEnrollmentService/GetOwnSectionEnrollment"
  headers:
    - name: Content-Type
      value: application/json
    - name: Cookie
      value: "sid={{gr_student_a_sid}}"
  body:
    type: json
    data: |-
      {
        "id": "{{gr_se_a_id}}"
      }

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.status
      operator: eq
      value: "passed"

docs: |
  Outcome check (boundary) — after correcting eval2 to 1.0, final = 4.0 exactly.
  The pass threshold is inclusive (≥ 4.0), so SE-A must still be "passed".
  Computed: 5.5×0.4 + 1.0×0.3 + 5.0×0.3 = 4.0.
  Mirrors: TestGradesOutcome_ExactBoundary4.
