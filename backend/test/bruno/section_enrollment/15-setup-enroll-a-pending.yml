info:
  name: 15 Setup Create Enrollment Student A
  type: http
  seq: 15

http:
  method: POST
  url: "{{host}}/enrollment.v1.EnrollmentService/CreateEnrollment"
  headers:
    - name: Content-Type
      value: application/json
    - name: Cookie
      value: "sid={{admin_sid}}"
  body:
    type: json
    data: |-
      {
        "studentId": "{{student_a_id}}",
        "programId": "{{prog_id}}",
        "year": 2026
      }

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.body;
        if (body && body.id) {
          bru.setVar("enroll_a_id", body.id);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.id
      operator: neq
      value: ""

docs: |
  Enrollment setup — creates a pending enrollment for student A (year=2026).
  Replaces: grpc enr CreateEnrollment student A in run_section_enrollment_flow.sh.
  Bash check: "CreateEnrollment student A -> pending"
