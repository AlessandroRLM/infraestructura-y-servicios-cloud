info:
  name: 05 Setup Student A Confirm Reset
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/auth.v1.AuthService/ConfirmPasswordReset"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "token": "{{cat_student_a_reset_token}}",
        "newPassword": "StudentACatalog123!"
      }

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  scripts:
    - type: before-request
      code: |-
        bru.setVar("cat_student_a_password", "StudentACatalog123!");
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |
  Catalog setup — step 2 of student A login chain: confirm password reset.
