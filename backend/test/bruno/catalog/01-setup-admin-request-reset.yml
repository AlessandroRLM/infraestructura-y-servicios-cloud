info:
  name: 01 Setup Admin Request Reset
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/auth.v1.AuthService/RequestPasswordReset"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "{{admin_email}}"
      }

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: false
  maxRedirects: 0

runtime:
  scripts:
    - type: before-request
      code: |-
        // Generate a run-scoped suffix so repeated runs against a persistent DB
        // do not collide on unique code/email constraints.
        const sfx = String(Date.now()).slice(-6);
        bru.setVar("sfx", sfx);
        bru.setVar("cat_prog_code", `CATPROG-${sfx}`);
        bru.setVar("cat_course_code", `CATCRS-${sfx}`);
        bru.setVar("cat_prog2_code", `CATPROG2-${sfx}`);
        bru.setVar("cat_course2_code", `CATCRS2-${sfx}`);
        console.log(`[catalog-setup] sfx=${sfx}`);
    - type: after-response
      code: |-
        const body = res.body;
        if (body && body.devToken) {
          bru.setVar("admin_reset_token", body.devToken);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |
  Catalog setup — step 1: request password reset for admin.
  Also seeds run-scoped variables (sfx, program/course codes).
  Uses cat_ prefixed vars to avoid collisions with other folder runs.
