// Feature: auth
// Status: scaffold — implementation pending
// Connects to: AuthService (gen/auth/v1)
// Note: login/logout actions only; session state lives in lib/session (cross-cutting)
export {};
