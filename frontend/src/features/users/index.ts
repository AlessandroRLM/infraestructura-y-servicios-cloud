// Feature: users
// Status: scaffold — implementation pending
// Connects to: ProfileService (gen/profiles/v1) — GetOwnProfile, student/teacher profiles
// Note: IAM admin (user/role management) is a pending slot until IamService proto exists
export {};
